import 'package:flutter/material.dart';

class ListClasesOpoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Listado de Clases Disponibles (Opositor)",
            style: TextStyle(fontSize: 18)),
      ),
    );
  }
}
