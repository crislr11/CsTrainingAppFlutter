import 'package:flutter/material.dart';

class MisClasesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mis Clases (Opositor)",
            style: TextStyle(fontSize: 18)),
      ),
    );
  }
}
